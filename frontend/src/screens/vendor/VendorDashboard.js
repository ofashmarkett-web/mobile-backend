import VendorTabsScreen from "./VendorTabsScreen";

// The vendor workspace: Home, Listings, Orders, Analytics and Profile tabs
// backed by the live vendor API (see VendorTabsScreen).
export default VendorTabsScreen;
