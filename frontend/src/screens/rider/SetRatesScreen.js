import React from "react";
import WorkspaceScreen from "../common/WorkspaceScreen";

const SetRatesScreen = ({ navigation }) => (
  <WorkspaceScreen
    navigation={navigation}
    nodeId="147-2868"
    title="Set rates"
    subtitle="Configure rider rate preferences, coverage area, and vehicle capacity."
  />
);

export default SetRatesScreen;
